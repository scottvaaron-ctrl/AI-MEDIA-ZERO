# Owner asset library

Drop your own JPG/PNG images here. Filenames are matched against scene visual queries
(e.g. `boston_harbor_1919.jpg`). Optional `credits.txt` lines: `filename | attribution text`.
Licensing is your responsibility; assets are tagged `owner-provided`.
