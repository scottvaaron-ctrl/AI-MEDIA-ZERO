# CLAUDE.md — AI Media Zero (`aimz`)

Standing briefing for any agent in this repo. Created 2026-09-13 by the owner's
vault session; the sections below the first are the only content so far —
extend it, don't replace it.

## Read first
1. `config/constitution.md` — owner rules the AI cannot edit. Never violate them.
2. `docs/HANDOFF.md` — ranked engineering work; the pattern for adding a platform.
3. `docs/CHECK_IN.md` — returning-owner checklist.
4. `docs/ARCHITECTURE.md`, `docs/COMPLIANCE.md` — design; verified platform rules.
5. `docs/CEO_BRIEF.md` — what the owner has been told; keep it current (see below).

Launch through the interpreter (`python -m aimz ...`); Windows Smart App Control
blocks the `aimz.exe` shim and the block is silent under Task Scheduler.


## Owner's company vault (Backhouse Group) — added 2026-09-13, decision D-008

The owner's company hub is an Obsidian vault at `C:\Users\scott\Documents\Backhouse_Group`. It is the CEO's
view across all ventures; this repo stays the engineering source of truth.

Rules for any agent working in this repo:

1. **Before starting**, if the vault folder is available to you, read
   `20-Ventures/AI Media Zero/AI Media Zero.md` (decisions, open questions) and check
   `00-Inbox/` for files named `PUSH — AI Media Zero — *.md`. Each is a decision the
   owner made in the vault that has not been applied here yet. Apply it to this
   repo's docs, then tell the owner which inbox file to delete. If the vault is
   not available, ask the owner whether there are pending PUSH items.
2. **Before ending any session that changes status, decisions needed, or
   risks**, update `docs/CEO_BRIEF.md`. That file is the only thing the vault ever
   copies from this repo. Keep its two-part shape (Part 1 for the CEO, Part 2
   for the CEO's agent) and put the date at the top.
3. **Never write anything else into the vault.** The vault session copies the
   brief; you do not push it.
4. The vault's `60-Registers/Decision Log.md` outranks any older statement in
   this repo when the two conflict. Flag the conflict; do not silently pick.
